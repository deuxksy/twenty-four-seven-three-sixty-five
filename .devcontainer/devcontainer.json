{
  "name": "OCI Free-Tier DevOps Workspace with SOPS",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "hashicorp.terraform",
        "redhat.ansible",
        "signageos.signageos-vscode-sops"
      ]
    }
  },
  "postStartCommand": "hash -r && if [ -n \"$SOPS_AGE_KEY\" ]; then echo \"$SOPS_AGE_KEY\" > keys.txt && chmod 600 keys.txt && echo 'export SOPS_AGE_KEY_FILE=\"$(pwd)/keys.txt\"' > .env.local && echo 'alias dec=\"sops -d .env.sops > .env\"' >> .env.local && echo 'alias enc=\"sops -e .env > .env.sops\"' >> .env.local && echo 'alias load=\"export \\$(cat .env | xargs)\"' >> .env.local && source .env.local && sops -d .env.sops > .env 2>/dev/null && echo '✅ 자동 Rebuild 및 SOPS 안착 성공!'; else echo '⚠️ SOPS_AGE_KEY 변수가 확인되지 않았습니다.'; fi",
  "remoteUser": "vscode"
}