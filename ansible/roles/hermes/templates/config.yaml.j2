# Hermes Agent config - Ansible 자동 생성
model:
  default: ark-code-latest
  base_url: https://ark.ap-southeast.bytepluses.com/api/coding

terminal:
  backend: local

approvals:
  mode: yolo

gateway:
  platforms:
    discord:
      extra:
        slash_commands: false
