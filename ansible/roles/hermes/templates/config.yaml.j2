# Hermes Agent config - Ansible 자동 생성
model:
  default: ark-code-latest
  provider: custom
  base_url: https://ark.ap-southeast.bytepluses.com/api/coding/v1
  api_key: {{ lookup('env', 'ANTHROPIC_API_KEY') }}

terminal:
  backend: local

approvals:
  mode: yolo

gateway:
  platforms:
    discord:
      extra:
        slash_commands: false
