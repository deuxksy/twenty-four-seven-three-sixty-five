services:
  hermes:
    image: {{ hermes_image | default('hermes:latest') }}
    container_name: hermes
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /data/hermes/data:/app/data
      - /data/hermes/config:/app/config
    environment:
      - TZ=Asia/Seoul
